export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in_progress',
  DONE = 'done',
}
//É um enum (tipo fixo) que define os valores permitidos para o status de uma tarefa.
// Serve para padronizar os status e evitar valores inválidos.
